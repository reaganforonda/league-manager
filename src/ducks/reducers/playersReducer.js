import axios from 'axios';

const INITIAL_STATE = {

}


export default function playersReducer(state=INITIAL_STATE, action) {
    switch(action.type) {
        default:
            return state
    }
}