import axios from 'axios';

const INITIAL_STATE = {

}

export default function fixtureReducer(state = INITIAL_STATE, action){
    switch(action.type){
        default:
            return state;
    }
}